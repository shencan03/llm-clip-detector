# Core transcription/model libraries
# whisperx is packaged manually via whisperx-x.x.x.tar.gz
# torch and torchaudio downloaded separately with pip download

# Tokenizer for chunking
tiktoken==0.9.0

# Sentence tokenizer
nltk==3.9.1

# Audio/video handling (if needed)
ffmpeg-python==0.2.0

# Whisper (if fallback needed)
openai-whisper

# Optional: Diarization auth helpers (if environment allows)
requests>=2.26.0

python-dotenv
